language: go

os:
  - linux
  - osx

sudo: false

go:
  - 1.9
  - tip

install:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure
  - go get golang.org/x/tools/cmd/goimports

script:
  - ./.travis.goimports.sh
  - go test -bench=. -v ./...
